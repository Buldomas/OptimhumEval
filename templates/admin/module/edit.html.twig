{% extends 'admin/base.html.twig' %}

{% block title %}Modification d'un module{% endblock %}
{% form_theme form 'admin/module/_collection.html.twig' %}

{% block body %}
    <div class="container">
        <h1 class="text-center mt-3">Modification du module : {{ module.titre }}</h1>

        {{ form_start(form) }}

        {{ form_widget(form) }}
        <div class="row">
            <div class="cols-6">
                <button type="submit" class="btn btn-primary">Enregistrer les Modifications</button>
            </div>
            <div class="cols-6">
                <a href="{{ path('admin_modules_index') }}" class="btn btn-secondary">Abandonner</a>
            </div>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}

{% block javascripts %}
    <script src="/js/module.js"></script>
{% endblock %}